---
name: challenge-builder-recovery
kind: workflow
description: "Fix and recover the active Challenge Builder worktree: resolve the current merge-conflict lane safely, infer the intended change from repository and planning evidence, preserve unrelated edits, finish the smallest coherent slice, and verify it with focused tests."
uses: ["../../../playbooks/bug-fix"]
details: ["agents/agent-organizer.md","agents/frontend-developer.md","agents/backend-architect.md","agents/error-coordinator.md","agents/test-writer-fixer.md"]
inputs: ["task","target repository"]
outputs: ["fix with reproduction and verification evidence"]
capabilities: ["root cause debugging","minimalism","testing","code review"]
---

# Instruction Set — challenge-builder-recovery

## Objective
Fix and recover the active Challenge Builder worktree: resolve the current merge-conflict lane safely, infer the intended change from repository and planning evidence, preserve unrelated edits, finish the smallest coherent slice, and verify it with focused tests.

## Next step
Open the target repository in your current host. Load this README.md as the operating workflow and execute the task.

Runtime paths belong to this execution only. If isolation is needed, place worktrees beside the target repository or under the user-configured worktree root. Select a scratch directory inside that workspace; do not fall back to another drive or the operating-system temp directory.

## Definition of Done
fix with reproduction and verification evidence

## Phases

1. reproduce
2. isolate cause
3. patch smallest source
4. verify

> Do not start the next phase until the current one is verifiably done. Validation: run the failing command or narrow regression test

## Agent roster (load a detail file only when you act as that agent)

- [agent-organizer](agents/agent-organizer.md) — orchestration, routing, teams
- [frontend-developer](agents/frontend-developer.md) — frontend, react, ui
- [backend-architect](agents/backend-architect.md) — backend, api, database
- [error-coordinator](agents/error-coordinator.md) — resilience, errors
- [test-writer-fixer](agents/test-writer-fixer.md) — tests, quality

## Capabilities
DIRF selected the best installed capability for each stage. Resolve each capability by name in the current host; provider values in the snapshot are discovery hints, not runtime requirements. Absent capabilities are never placed in the executable flow.

## Skill flow
Reach for skills in this order — each has a reason for its place in the sequence:

**diagnose**
- ✅ `systematic-debugging` — Build a tight feedback loop before theorizing.
- ✅ `ponytail` — Patch the smallest shared source of the bug.
**fix**
- ✅ `tdd` — Lock the bug down with a red-green regression test.
**review**
- ✅ `code-review` — Review the fix for regressions and scope.

## Policy
Read [policy.md](policy.md) before editing.

## If blocked
if reproduction is missing, gather exact error and command first

_Generated by amf-dirf. schema_version 4. Rebuild locally to refresh routing and capabilities._