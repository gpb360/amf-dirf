---
name: accessibility-check
kind: skill
description: "Verify keyboard, contrast, semantics, and assistive-tech support"
uses: []
details: []
inputs: ["interface under review"]
outputs: ["accessibility findings"]
capabilities: ["accessibility"]
---

# Accessibility check

Walk the interface with keyboard only: every action reachable, focus visible, no traps. Check semantic structure — headings, labels, roles — and text contrast at required ratios. Verify images and controls carry accessible names, and that motion respects reduced-motion preferences.
